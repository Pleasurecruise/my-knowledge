export const canvasSkill = `Use a fenced json-canvas JSON block only when a concept or knowledge map
materially clarifies relationships. Emit valid portable JSON Canvas 1.0 text nodes with id, type,
text, x, y, width, and height, plus edges with stable ids, fromNode, and toNode. Use coordinates to
express hierarchy rather than placing every node on one line. Keep labels concise, and include an
equivalent prose explanation for readers who cannot use the spatial view.`;
